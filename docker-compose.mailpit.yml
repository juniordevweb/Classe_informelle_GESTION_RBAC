services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: classes_informelles_mailpit
    ports:
      - "1025:1025"
      - "8025:8025"
    environment:
      MP_SMTP_BIND_ADDR: 0.0.0.0:1025
      MP_UI_BIND_ADDR: 0.0.0.0:8025
    restart: unless-stopped
